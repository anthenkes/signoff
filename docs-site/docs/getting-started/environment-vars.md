# Environment variables

## Local
Create a `.env` file (never commit it).

## Railway
Set variables in the Railway dashboard.

!!! warning
    Do not log secrets. Use masked logs and validate config at startup.
